name = Simple LDAP Delete Blocked User
description = Deletes a user from LDAP when set to Blocked in Backdrop. This keeps the directory clean, and when restoring the account to Active, the user will be resynced to LDAP by the Simple LDAP User module.
package = LDAP Helpers
dependencies[] = simple_ldap_user
backdrop = 1.x
type = module
tags[] = Account Management
