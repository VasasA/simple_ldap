[SimpleLdapRoleCreateBackdropRoleTestCase]
name = Create Backdrop Role
description = Tests that LDAP groups are created appropriately when a Backdrop role is created.
group = Simple LDAP Role
file = simple_ldap_role.test

[SimpleLdapRoleDeleteBackdropRoleTestCase]
name = Delete Backdrop Role
description = Tests that LDAP groups are deleted when a Backdrop role is deleted.
group = Simple LDAP Role
file = simple_ldap_role.test

[SimpleLdapRoleChangeBackdropUserRoleTestCase]
name = Change Backdrop Role
description = Changes the roles on a Backdrop user, and tests that the LDAP is updated appropriately.
group = Simple LDAP Role
file = simple_ldap_role.test

[SimpleLdapRoleModifyLdapGroup]
name = Modify Backdrop Role
description = Modifies LDAP groups, and verifies that Backdrop roles are updated on cron runs.
group = Simple LDAP Role
file = simple_ldap_role.test

[SimpleLdapRoleChangeLdapGroupMembership]
name = Change LDAP Group Membership
description = Changes an LDAP user's group membership, and verifies that Backdrop role membership is updated.
group = Simple LDAP Role
file = simple_ldap_role.test
